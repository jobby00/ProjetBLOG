{% extends "layout.html.twig" %}

{% block title %}Tous les Articles{% endblock %}

{% block content %}
<section class="main-container container">
    <div class="container">
        <div class="row">
        <!-- main start -->
            <!-- ================ -->
            <div class="main col-md-12">
                <div class="section clearfix object-non-visible" data-animation-effect="fadeInUpSmall" data-effect-delay="200">
                    <div class="container">
                        <h1 class="text-center">Tous les articles</h1>
                        <div class="row">
                            {% for article  in articles%}
                                <div class="space hidden-md hidden-lg"></div>
                                <div class="col-md-6">
                                    <h2 class="text-center"><a class="articleTitle" href="{{ path('article', { 'id':article.id }) }}">{{ article.title }}</a></h2>
                                    <p class="lead text-center"><i>{{ article.date}}</i></p>
                                    <p class="space-top">{{ article.content| striptags|slice(0, 450) |raw |raw}} ...
                                        <a class="allarticles" href="{{ path('article', { 'id':article.id }) }}"><i class="material-icons">Lire la suite</i> </a></p>
                                </div>
                            {% endfor %}
                            <div class="space hidden-md hidden-lg"></div>
                        </div>
                    </div>
                </div>
                <!-- pagination end -->
            </div>
        </div>
    </div>
</section>
<!-- section end -->
{% endblock %}